rd_("AlCreate a new kprobe builder.AlCreate a new kprobe manager.AoCreate a new kretprobe builder.AkCall the callback function.AoReturns the argument unchanged.000000000BaCalls <code>U::from(self)</code>.000000000BcThe x86_64 implementation of Probe.CbThe kprobe structure for the current architecture.BoThe CPU register state for x86_64 architecture.CbThe uprobe structure for the current architecture.CfGet a reference to the data as a <code>dyn Any</code>.AgEnable the probe point.CjA Rust library for dynamic kernel probing (kprobes and \xe2\x80\xa6BkGet the underlying kprobe of the kretprobe.BiGet the function name of the probe point.AhDisable the probe point.AgThe retprobe structure.CdAn enum representing either a kprobe or a kretprobe.CiThe retprobe implementation for the current architecture.CeThe kretprobe structure for the current architecture.BlThe user data associated with a probe point.CeThe uretprobe structure for the current architecture.BkBuild the kprobe with a specific user data.BdSet the user data for the kretprobe.BaThe basic information of a probe.CgThe probe point structure for the current architecture.BdCheck if the probe point is enabled.ChGet the number of kprobes registered at a breakpoint \xe2\x80\xa6CiGet the address of the instruction that should be probed.DgCopy memory from source to destination. If <code>user_pid</code> is \xe2\x80\xa6BoGet the probe point associated with this probe.BaGet the probe point of the probe.BhBuild the kprobe with enable or disable.0B`Build the kprobe with an offset.AoBuild the kprobe with a symbol.0BfThe callback function type for events.BbThe builder for creating a kprobe.AfA manager for kprobes.BaInsert a kprobe into the manager.BbCheck if the probe is a kretprobe.BaRemove a kprobe from the manager.BhThe probe point for x86_64 architecture.AhA list of kprobe points.CkGet the list of kprobes registered at a breakpoint address.CfGet the list of kprobes registered at a debug address.BeBuild the kprobe with user mode flag.BdThe builder for creating a retprobe.BkGet the return value from the rax register.AbRegister a kprobe.AbRegister a uprobe.CcThe kretprobe builder for the current architecture.BgThe type of the probe handler function.AkThe instance of a retprobe.AnCall the pre handler function.BkGet the return value from the rdx register.BmBuild the kprobe with a pre handler function.BiSet the return handler for the kretprobe.BgBuild the kprobe with a symbol address.0AoCall the post handler function.AdUnregister a kprobe.AdUnregister a uprobe.BnBuild the kprobe with a post handler function.BnThe auxiliary operations required for kprobes.B`Call the fault handler function.AeRegister a kretprobe.BhSet the entry handler for the kretprobe.:BaCall the event callback function.CaBuild the kprobe with an event callback function.0AgUnregister a kretprobe.BkDeallocate user executable memory(one page)BiAllocate user executable memory(one page)BfDeallocate executable memory(one page)BeRegister the event callback function.0BdAllocate executable memory(one page)CcRun kprobe which has been registered on the address0ChEnable or disable write permission for the specified \xe2\x80\xa6BgUnregister the event callback function.0CcRun uprobe which has been registered on the address0BnPop a kretprobe instance from the current taskBoInsert a kretprobe instance to the current task")